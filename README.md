```bash
zig build --release=fast
```

## For Mac users
copy the `zig-out/bin/zrrp` to `~/bin` and run via terminal using `zrrp`

## For Windows users
create an `Apps` directory under your user and add it to the `PATH`
